# Copy to .env.local for local development.
# For production, this is baked into the Docker image at build time.
# When frontend & API are on the same origin, set to /api.
VITE_API_URL=http://localhost:8888/api
